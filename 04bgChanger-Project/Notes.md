# Note:-

```
-> In this directory we build an basic backGround color changer project..
-> change background on click the button
-> clearing concept/behaviour of onClink function
-> onClick accept full function not the refrence so we pass arrow function as an argument
```
